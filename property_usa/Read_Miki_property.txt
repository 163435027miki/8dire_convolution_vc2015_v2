1行目の値は
入力画像サイズx,入力画像サイズy,基準ベクトル作成flag,基準ベクトル取得座標x,基準ベクトル取得座標y,カーネルサイズ
となっている．この内，基準ベクトル関係は作成時以外は無関係となる

2行目の値は
使用する（作成する）基準ベクトルのディレクトリとなる．

3行目は入力画像

4行目以降は使用するカーネルを指定

propertyファイル名例として
kernelを使用 : property_3k_conv_sd0.txt
sobelを使用  : property_3×3sobel_conv_sd0.txt
を推奨